{
  "name": "aptpref",
  "version": "1.0.0",
  "description": "Converts apt list output to preferences file",
  "main": "index.js",
  "bin": "index.js",
  "scripts": {
    "build": "pkg --compress GZip .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Li Shing To <shingto@shingto.com>",
  "license": "MIT",
  "dependencies": {
    "yargs": "^17.0.1"
  },
  "devDependencies": {
    "pkg": "^5.3.1"
  },
  "pkg": {
      "targets": ["node12-linux-x64"],
      "outputPath": "bin"
  }
}
